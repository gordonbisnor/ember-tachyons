import Ember from 'ember';
import layout from '../templates/components/t-profile-card-name';

export default Ember.Component.extend({
  classNames: [
    'f4', 'b', 'ma0'
  ],
  tagName: 'p',
  layout
});
