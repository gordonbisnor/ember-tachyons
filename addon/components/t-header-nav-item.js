import Ember from 'ember';
import layout from '../templates/components/t-header-nav-item';

export default Ember.Component.extend({
  classNames: [
    'dib', 'mr3'
  ],
  tagName: 'span',
  layout
});
